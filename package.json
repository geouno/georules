{
  "name": "georules",
  "version": "0.0.1",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/bun": "latest"
  },
  "private": true,
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "test": "bun --filter '*' test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
